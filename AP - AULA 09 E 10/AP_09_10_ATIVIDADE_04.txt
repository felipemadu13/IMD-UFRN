var x1 := leia_inteiro
var acima := 0
var abaixo := 0
enquanto x1 <> 0 faça
se x1 > 0 então
acima := acima + 1
senão
abaixo := abaixo + 1
fim
var x1 := leia_inteiro
fim
escreva "Números maiores que zero {acima}"
escreva "Números menores que zero {abaixo}"
